const dashboard = "/dashboard";
const login = "/login";
const signup = "/signup";
const forgotPassword = "/forgot-password";

const pathUrls = {
  dashboard,
  login,
  signup,
  forgotPassword
};

export default pathUrls;
